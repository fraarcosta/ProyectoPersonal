apiVersion: v1
kind: Service
metadata:
  name: agent-oferta
  namespace: akena-dev
  labels:
    app: akena-agent-oferta
    agentic.io/agent-type: specialized
    agentic.io/agent-name: agent-oferta
    app.kubernetes.io/name: agent-oferta
    app.kubernetes.io/part-of: akena
    environment: dev
spec:
  type: ClusterIP
  selector:
    app: akena-agent-oferta
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP

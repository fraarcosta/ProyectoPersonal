apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: akena-dev

resources:
  - namespace.yaml
  - configmap.yaml
  - secret.yaml
  - deployment.yaml
  - service.yaml

commonLabels:
  app.kubernetes.io/managed-by: kustomize
  project: akena

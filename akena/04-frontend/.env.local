# Akena — Local development environment
# This file is NOT committed to git (.gitignore)

# Orchestrator URL — proxied through Vite dev server
VITE_ORCHESTRATOR_URL=/api/orchestrator

# Set to "true" to keep using the local mock instead of the real backend
VITE_USE_MOCK_CHAT=false
